using UnityEngine;

/**
 * Add this to an object to select it instead of its children. Useful when a mesh or sprite is a child of what you
 * will actually want to select.
 */
[SelectionBase]
public class SelectionBase : MonoBehaviour
{
}
